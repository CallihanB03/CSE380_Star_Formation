#pragma once

// constexpr float RHO_THRESHOLD = 1.0f;   // example
// constexpr float MASS_TO_STAR = 1.0f;    // example
constexpr float DIV_THRESHOLD = 0.0f;   // require converging flow
constexpr float H = 0.03f;              // default smoothing length
